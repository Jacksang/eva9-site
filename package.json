{
  "name": "eva9-site",
  "version": "0.1.0",
  "description": "blog.eva9.ai — Jacky Chen's bilingual personal website",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "node --test --test-concurrency=1 'src/__tests__/**/*.test.ts'",
    "test:integration": "node --test --test-concurrency=1 'tests/integration/**/*.test.ts'",
    "lint": "eslint src/ --ext .ts,.astro",
    "typecheck": "astro check"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.0",
    "astro": "^5.0.0",
    "typescript": "^5.6.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "@types/node": "^25.9.2",
    "eslint": "^9.0.0",
    "prettier": "^3.4.0",
    "prettier-plugin-astro": "^0.14.0",
    "tailwindcss": "^4.0.0",
    "vitest": "^2.0.0"
  }
}
